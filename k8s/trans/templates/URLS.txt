{{- if .Values.generateURLS }}
{{- if .Values.ingress.enabled }}
{{- $hostPrefix := include "trans.hostPrefix" . -}}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  {{ printf "%s.%s" $hostPrefix $host.host }}
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
