apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trans.fullname" . }}-test-connection"
  labels:
{{ include "trans.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "trans.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
